{{- if .Values.controlPlane.enabled }}
{{- $ns := include "k8squad.namespace" . }}
{{- $c := "memory" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ksquad-memory
  namespace: {{ $ns }}
  labels:
    {{- include "k8squad.componentLabels" (dict "root" $ "component" $c) | nindent 4 }}
spec:
  replicas: {{ .Values.controlPlane.memory.replicas | default 1 }}
  selector:
    matchLabels:
      {{- include "k8squad.componentSelectorLabels" (dict "root" $ "component" $c) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "k8squad.componentSelectorLabels" (dict "root" $ "component" $c) | nindent 8 }}
    spec:
      serviceAccountName: ksquad-memory
      securityContext:
        runAsNonRoot: true
      containers:
      - name: memory
        image: {{ include "k8squad.image" (dict "root" $ "component" $c) }}
        imagePullPolicy: {{ .Values.controlPlane.image.pullPolicy | default "IfNotPresent" }}
        ports:
        - name: http
          containerPort: 8080
        env:
        - name: HTTP_PORT
          value: "8080"
        {{- include "k8squad.databaseEnv" . | nindent 8 }}
        {{- include "k8squad.otelEnv" . | nindent 8 }}
        {{- with .Values.controlPlane.memory.extraEnv }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        # /healthz is liveness-only; /readyz gates on store.Ready (a DB ping) so a
        # Postgres-down pod is pulled from Service endpoints instead of routed traffic.
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 10
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: [ALL]
        resources:
          {{- toYaml (.Values.controlPlane.memory.resources | default dict) | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: ksquad-memory
  namespace: {{ $ns }}
  labels:
    {{- include "k8squad.componentLabels" (dict "root" $ "component" $c) | nindent 4 }}
spec:
  selector:
    {{- include "k8squad.componentSelectorLabels" (dict "root" $ "component" $c) | nindent 4 }}
  ports:
  - name: http
    port: 8080
    targetPort: http
{{- end }}
